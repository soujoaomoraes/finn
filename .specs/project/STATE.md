# STATE — FinLedger

## Memory
- O aplicativo é um port de uma versão web (HTML vanilla) para desktop usando Tauri v2, Rust e SQLite.
- O HTML original com o design do app está localizado em `.specs/features/v0-migration/mockup.html`.
- Decidimos migrar os arquivos de especificação para a estrutura do `tlc-spec-driven` (`.specs/`) para gerenciar as features e arquitetura com mais organização.
- A Task 01 do projeto inicial (criar o projeto) foi realizada. A Task 02 parecia concluída mas foi verificada e revertida, pois o plugin SQL não estava instalado.

## Blockers / Open Questions
- Nenhum. O próximo passo é rodar a Task 02 (Instalar dependências e configurar Tauri).

## Deferred Ideas
- Nenhuma.
