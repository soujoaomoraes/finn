# STATE — FinLedger

## Memory
- O aplicativo utiliza Tauri v2 com Rust e SQLite no backend.
- O frontend foi refatorado na versão V0.5 para utilizar Vite com Vanilla JS modularizado e CSS segmentado por componentes/telas.
- A versão V0.5 foi concluída, validada e o código salvo no GitHub.
- A arquitetura atual baseia-se em ES Modules locais gerados pelo Vite e chamadas "invoke" que se conectam ao core Rust.

## Blockers / Open Questions
- Nenhum. O próximo passo lógico é planejar a V1 (Backup + Recorrentes).

## Deferred Ideas
- Nenhuma.
