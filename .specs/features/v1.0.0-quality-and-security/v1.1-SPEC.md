# SPEC.md — FinLedger V1.1
**Versão:** V1.1 — Redesign Visual + Segurança + Qualidade  
**Plataforma:** Desktop — Tauri (Windows / macOS / Linux)  
**Última atualização:** Maio 2026  
**Referências:** `REFERENCE.md` (v1.1-quality-and-security), mockups aprovados (Maio 2026)

---

## 1. Visão Geral

A V1.1 é uma versão de **qualidade e redesign**. Nenhuma funcionalidade nova de produto é adicionada — o objetivo é elevar a interface visual ao design system warm dark aprovado nos mockups, corrigir débitos técnicos de segurança identificados na V1, adicionar observabilidade ao backend e estabelecer cobertura de testes automatizados.

**O que muda na V1.1:**
1. **Redesign visual completo** — aplicação do design system warm dark em todas as telas
2. **Dashboard redesenhado** — layout, gráfico e painel de categorias conforme mockup aprovado
3. **Drawer lateral** — Nova/Editar Transação e Nova/Editar Recorrente migram para drawer, removendo a tela dedicada "Nova Transação"
4. **Segurança** — renovação automática de token OAuth, armazenamento em cofre nativo, SQLCipher
5. **Limpeza de código** — remoção de dead code em `oauth.rs`
6. **Observabilidade** — logging estruturado com `log` + `env_logger`
7. **Testes automatizados** — unitários no backend, mock de banco, E2E com WebdriverIO

**Fora do escopo da V1.1:**
- Novas funcionalidades de produto
- Sincronização bidirecional entre dispositivos
- Fila granular de operações de backup
- Modo light
- Notificações do sistema operacional

---

## 2. Design System — Tokens

A V1.1 substitui integralmente o sistema de cores e tipografia atual.

### 2.1 Superfícies (Warm Dark)

| Token | Valor | Uso |
|---|---|---|
| `--base` | `#0d0c0a` | Fundo da janela |
| `--s1` | `#131110` | Sidebar, nav lateral |
| `--s2` | `#1a1815` | Cards, painéis, chart |
| `--s3` | `#201e1a` | Inputs, rows em hover |
| `--s4` | `#272420` | Modais, drawers, tooltips |
| `--b1` | `#232018` | Borda principal de cards |
| `--b2` | `#2e2b25` | Inputs, separadores |
| `--b3` | `#3a362e` | Hover states elevados |

**Princípio:** nenhum preto puro. Cada camada tem temperatura marrom levemente quente. Em OLED o resultado é veludo, não plástico.

### 2.2 Texto (Warm Neutrals)

| Token | Valor | Uso |
|---|---|---|
| `--t1` | `#ede8df` | Texto principal |
| `--t2` | `#b5a898` | Texto secundário, nav inativo |
| `--t3` | `#6b5f52` | Subtítulos, meta info |
| `--t4` | `#3d342c` | Labels uppercase, grid lines |

### 2.3 Cores Semânticas

| Token | Valor | Uso |
|---|---|---|
| `--orange` | `#f97316` | CTA, logo, pip ativo — **nunca em valores monetários** |
| `--og06` | `rgba(249,115,22,.06)` | Fundo nav item ativo |
| `--og10` | `rgba(249,115,22,.10)` | Fundo de badges laranja |
| `--og20` | `rgba(249,115,22,.22)` | Borda de badges laranja |
| `--green` | `#4ade80` | Receitas, valores positivos |
| `--gn10` | `rgba(74,222,128,.10)` | Fundo ícone receita |
| `--red` | `#f87171` | Despesas, valores negativos |
| `--rd10` | `rgba(248,113,113,.10)` | Fundo ícone despesa |
| `--amber` | `#fbbf24` | Saldo do mês |
| `--ab10` | `rgba(251,191,36,.10)` | Fundo ícone saldo |
| `--blue` | `#60a5fa` | Acumulado, status sync |
| `--bl10` | `rgba(96,165,250,.10)` | Fundo ícone acumulado |

### 2.4 Tipografia

Fonte única: **Inter** (Google Fonts). Substituição total de DM Serif Display e DM Sans.

| Nível | Tamanho | Peso | Letter-spacing | Uso |
|---|---|---|---|---|
| Display / KPI | 24px | 600 | -0.6px | Valores grandes no gráfico |
| Heading / Page | 19–21px | 600 | -0.5px | Títulos de tela |
| Card value | 20px | 600 | -0.7px | Valores nos cards |
| Body / UI | 12–12.5px | 400–500 | 0 | Texto corrido, linhas de tabela |
| Meta / Caption | 10–11px | 400 | 0 | Datas, categorias, subtextos |
| Label / Tag | 9px | 500 | +0.7–0.8px uppercase | Labels de seção, headers de tabela |
| Nav section | 7.5–8px | 600 | +1.5px uppercase | Seções da sidebar |

### 2.5 Espaçamento (escala 4pt)

`4 · 8 · 10 · 12 · 13 · 16 · 18 · 20 · 22 · 26 · 28px`

### 2.6 Border Radius

| Valor | Uso |
|---|---|
| 2px | Pip da sidebar, barras de progresso |
| 4px | Labels de tooltip no gráfico |
| 8px | Nav items, botões secundários |
| 9px | Botão CTA, month-nav, inputs, selects |
| 10px | Icon badges de transação |
| 12px | Cards, painéis, tabelas |
| 14px | App shell, modais |
| 999px | Badges pill (receita/despesa/status) |

### 2.7 Sidebar

Largura: **192px** (fixo, sem colapso na V1.1).

---

## 3. Layout e Navegação

### 3.1 Shell

```
┌──────────────┬────────────────────────────────────────┐
│   Sidebar    │              Main                       │
│   192px      │  Topbar (título + ações)                │
│              ├────────────────────────────────────────┤
│              │  Scrollable content                     │
│              │                                         │
└──────────────┴────────────────────────────────────────┘
```

### 3.2 Sidebar

```
VISÃO GERAL
  Dashboard

LANÇAMENTOS
  Transações
  Recorrentes

CONFIGURAÇÕES
  Categorias
  Importar / Exportar
  Backup
```

- Item ativo: fundo `--og06` + pip laranja 3px à esquerda + texto `--t1`
- Item inativo: cor `--t3`, sem fundo
- "Nova Transação" **removido** da sidebar — acesso somente via botão nas telas de Transações e Dashboard

### 3.3 Navegação por Drawer

Nova Transação e Nova/Editar Recorrente abrem em **drawer lateral** (380px, desliza da direita). Overlay escurece o conteúdo. Fechar: botão ×, tecla Escape, clique no overlay.

---

## 4. Telas

### 4.1 Dashboard

**Topbar:** título "Dashboard" · subtítulo "Resumo financeiro mensal" · month-nav (‹ Mai 2026 ›) · botão "+ Nova transação" (abre drawer)

**Grid de cards — 4 colunas iguais:**

| Card | Valor | Cor |
|---|---|---|
| Receitas | Total do mês | `--green` |
| Despesas | Total do mês | `--red` |
| Saldo do mês | Receitas − Despesas | `--amber` |
| Acumulado | Soma total histórica | `--blue` |

Cada card: label 9px uppercase `--t4` + ícone SVG no canto direito (fundo translúcido 10% da cor semântica) + valor 20px 600 + sublabel 10px `--t4`.

**Grid inferior — mesmo grid de 4 colunas, painéis span 2:**

**Painel esquerdo — Evolução do acumulado (span 2):**
- Label "Evolução do acumulado" + badge "+ X% vs mês anterior" (laranja)
- KPI: valor acumulado em 24px 600 laranja
- Sublabel: "acumulado até [Mês] [Ano]"
- Gráfico SVG de linha laranja com gradiente de área, últimos 6 meses
- Cada ponto tem pill de valor acima (pill fill `--s4`, texto laranja)
- Ponto ativo (mês atual): círculo sólido laranja com halo duplo
- Grid lines horizontais sutis em `--s3`
- Labels de mês abaixo; mês atual em laranja
- **Pill de Abr deslocado à esquerda** para não colidir com pill de Mai

**Painel direito — Gastos por categoria (span 2):**
- Header: label "Gastos por categoria" + link "ver todas →" (laranja)
- Lista de categorias de despesa do mês, ordenadas por valor decrescente
- Cada linha: dot colorido · nome · valor · percentual sobre receita total · barra de progresso
- **Percentual sobre receita total** (não sobre total de despesas): mostra quanto cada categoria consumiu das receitas
- Barra de progresso: height 3px, fill colorido opacity 0.75, track `--b2`
- **Rodapé "Saldo do mês":** label + "X% gastos · Y% restante" + barra empilhada 4px mostrando cada categoria + restante em verde
- Estado vazio: "Sem gastos neste mês"

**Interação do mês:** month-nav atualiza cards, gráfico e painel de categorias em tempo real.

---

### 4.2 Transações

**Topbar:** título "Transações" · subtítulo "Todos os seus lançamentos" · botão "+ Nova transação" (abre drawer)

**Filtros (4 em linha):**

| Filtro | Tipo | Padrão |
|---|---|---|
| Mês | Select dinâmico com meses disponíveis + "Todos" | Mês atual |
| Tipo | Select: Todos / Receita / Despesa | Todos |
| Categoria | Select: Todas + categorias | Todas |
| Busca | Input texto (substring, case-insensitive) | Vazio |

**Tabela:**

Colunas: Data · Descrição · Categoria · Tipo · Valor · Ações

- **Data:** DD/MM/YYYY · cor `--t3` · 11.5px
- **Descrição:** texto principal `--t1` 500 + obs em `--t4` 10px abaixo
- **Categoria:** dot colorido + nome
- **Tipo:** badge pill — `badge-receita` (verde translúcido) ou `badge-despesa` (vermelho translúcido)
- **Valor:** alinhado à direita · `+R$ X,XX` verde / `−R$ X,XX` vermelho · 12.5px 500
- **Ações:** icon-btn editar (lápis) + icon-btn excluir (lixeira) · 28×28px · border-radius 7px · hover muda cor
- Linha hover: background `--s3`
- Ícone de recorrência discreto ao lado da descrição se `recorrente_id` preenchido · tooltip "Gerada automaticamente"

**Rodapé da tabela:**
- Esquerda: "X transações" em `--t4`
- Direita: total receitas em verde · separador · total despesas em vermelho

**Estado vazio:** ícone + "Nenhuma transação encontrada"

---

### 4.3 Drawer — Nova / Editar Transação

**Header do drawer:** título "Nova Transação" ou "Editar Transação" · sublabel "Organize seus lançamentos" · botão × fechar

**Campos:**

| Campo | Componente | Validação |
|---|---|---|
| Tipo | Tabs "Despesa" / "Receita" | Obrigatório. Padrão: Despesa |
| Descrição | Input texto | Obrigatório |
| Valor | Input numérico com prefixo "R$" | Obrigatório, > 0 |
| Data | Input date | Obrigatório. Padrão: hoje |
| Categoria | Select filtrado pelo tipo | Obrigatório |
| Observação | Textarea | Opcional |

**Footer do drawer:** botão "Salvar" (primário, flex:1) · botão "Cancelar" (secundário)

**Ao salvar:** toast "Transação salva!" / "Transação atualizada!" · drawer fecha · lista atualiza

**Validações (toast de erro):** "Informe a descrição" · "Informe um valor válido" · "Informe a data"

---

### 4.4 Recorrentes

**Topbar:** título "Recorrentes" · subtítulo "Transações automáticas agendadas" · botão "+ Nova recorrente" (abre drawer)

**Lista:**

Cada item: icon-badge colorido (receita laranja, despesa vermelho) · descrição + meta (frequência · dia · categoria) · badge de status · valor · botões de ação

- **Status:** `status-ativa` (verde) ou `status-pausada` (cinza)
- **Valor:** `+R$ X` verde para receita / `−R$ X` vermelho para despesa · padrão `--t3` se pausada
- **Ações:** icon-btn editar · icon-btn pausar/retomar · icon-btn excluir
- Bordas entre itens em `--b1`

**Estado vazio:** ícone + "Nenhuma transação recorrente cadastrada"

**Modal de exclusão:** "Excluir esta recorrente não remove as transações já geradas. Deseja continuar?" · botões Cancelar + Excluir (vermelho)

---

### 4.5 Drawer — Nova / Editar Recorrente

**Header:** "Nova Recorrente" ou "Editar Recorrente" · sublabel "Transação automática agendada"

**Campos:**

| Campo | Componente | Visibilidade |
|---|---|---|
| Tipo | Tabs Despesa / Receita | Sempre |
| Descrição | Input texto | Sempre |
| Valor | Input numérico R$ | Sempre |
| Categoria | Select filtrado pelo tipo | Sempre |
| Frequência | Select: Mensal / Semanal / Diária / Anual | Sempre |
| Dia do mês | Input 1–31 | Mensal e Anual |
| Dia da semana | Select Segunda…Domingo | Semanal |
| Mês | Select Janeiro…Dezembro | Anual |
| Observação | Textarea | Sempre (opcional) |

`criada_em` definido automaticamente, não exposto no formulário.

---

### 4.6 Categorias

**Topbar:** título "Categorias" · subtítulo "Organize seus lançamentos por tipo" · botão "+ Nova categoria" (abre modal)

**Layout 2 colunas:** Despesas | Receitas

Cada coluna:
- Label da seção em 9px uppercase `--t4`
- Card com lista de categorias
- Cada linha: swatch colorido (10px dot) · nome · icon-btn editar · icon-btn excluir
- Estado vazio por coluna: "Nenhuma categoria"

**Modal Nova / Editar Categoria:**
- Título: "Nova Categoria" ou "Editar Categoria"
- Campo Nome (obrigatório)
- Tabs Despesa / Receita para tipo
- Color picker: 12 swatches, seleção única com borda branca + escala 1.1
- Botões Cancelar + Salvar
- Validação: toast "Informe o nome"
- Fechar: overlay ou Escape

**Paleta das 12 cores (inalterada):**
`#f87171 · #fb923c · #fbbf24 · #a3e635 · #4ade80 · #34d399 · #22d3ee · #60a5fa · #818cf8 · #c084fc · #f472b6 · #94a3b8`

---

### 4.7 Importar / Exportar

**Topbar:** título "Importar / Exportar" · subtítulo "Gerencie seus dados em planilha"

**Layout 2 colunas lado a lado:**

**Coluna esquerda — Exportar dados:**
- Sublabel de seção + descrição
- Inputs de data início e data fim (padrão: primeiro e último dia do mês atual)
- Botão "Exportar CSV" (primário)
- Ao clicar: abre diálogo nativo de salvar arquivo · nome sugerido `finledger-YYYY-MM-DD_YYYY-MM-DD.csv`
- Toast sucesso: "CSV exportado com sucesso"
- Toast erro: "Nenhuma transação encontrada no período" / "Erro ao exportar"

**Coluna direita — Importar dados:**
- Dropzone clicável com drag & drop · aceita `.xlsx .xls .csv`
- Visual muda ao arrastar sobre a área (borda laranja + fundo `--og06`)
- Tabela de formato esperado abaixo da dropzone

**Formato CSV exportado:**
```
Data,Descrição,Valor,Tipo,Categoria,Observação
15/01/2026,Supermercado,250.00,despesa,Alimentação,
```

---

### 4.8 Backup

**Topbar:** título "Backup" · subtítulo "Sincronize seus dados com o Google Drive" · cloud indicator (quando sincronizando)

**Card de status — estado Conectado:**
- Label "Status da sincronização" + badge "Conectado" (verde)
- Email da conta Google
- "Último backup: DD/MM/YYYY às HH:MM" (formatado, não ISO bruto)
- Botões: "Desconectar" (secundário) · "Fazer backup agora" (primário)

**Card de estado — Desconectado:**
- Descrição do que é o backup
- Botão "Conectar com Google" (primário)
- Estado conectando: loading no botão

**Card Restaurar:**
- Descrição do risco
- Botão "Restaurar do Drive" (danger)
- Modal de confirmação com aviso explícito de que dados locais serão substituídos

**Cloud indicator (topbar):** pill azul com ícone de nuvem + "Sincronizando" enquanto upload em andamento · some ao concluir

**Estados de erro:** mensagem inline + botão "Tentar novamente"

---

## 5. Componentes Globais

### 5.1 Toast

- Canto inferior direito
- Desaparece em 2.8s
- Animação: `slideUp` 0.25s ease
- Dot colorido à esquerda (verde/vermelho/âmbar conforme contexto)
- Background `--s4` · borda `--b2` · border-radius 10px

### 5.2 Drawer

- Largura: 380px · desliza da direita · `transform translateX` 0.25s ease
- Overlay: `rgba(0,0,0,.5)` com `fadeIn` 0.2s
- Fechar: × no header · Escape · clique no overlay
- Header: título + sublabel + botão × (28×28px · `--s3`)
- Body: scrollável · `gap: 16px` entre campos
- Footer: fixo na base · borda superior `--b1` · botões alinhados à esquerda (Salvar flex:1, Cancelar fixo)

### 5.3 Modal

- Centralizado · largura 380px · `--s2` · border-radius 14px · borda `--b2`
- Overlay: `rgba(0,0,0,.6)` · fechar clicando fora ou Escape
- Título 15px 600 · corpo 12.5px `--t3` · ações alinhadas à direita

### 5.4 Icon Button

- 28×28px · border-radius 7px · borda `--b2` · cor `--t3`
- Hover: background `--s3` · cor `--t1`
- Variante danger hover: background `--rd10` · cor `--red` · borda `rgba(248,113,113,.2)`

### 5.5 Badge Pill (tipo)

- Padding `3px 10px` · border-radius 999px · font-size 11px · weight 500
- Receita: `--gn10` bg · `--green` text · borda `rgba(74,222,128,.15)`
- Despesa: `--rd10` bg · `--red` text · borda `rgba(248,113,113,.15)`

### 5.6 Status Chip (estado operacional)

- Padding `3px 9px` · border-radius 6px · font-size 11px
- Dot 5×5px colorido à esquerda
- Ativa/Conectado: `--gn10` bg · `--green`
- Pausada: cinza translúcido · `--t3`
- Sincronizando: `--bl10` bg · `--blue`
- Erro: `--rd10` bg · `--red`

### 5.7 Formatação de Valores

- Moeda: `R$ X.XXX,XX` — `toLocaleString('pt-BR', {style:'currency', currency:'BRL'})`
- Datas exibidas: `DD/MM/YYYY`
- Datas meta: `DD/MM/YYYY às HH:MM` (timestamps de backup)
- Datas armazenadas: `YYYY-MM-DD`

### 5.8 Estados Vazios

Todas as listas e tabelas exibem: ícone SVG em box `--s3` · título `--t3` 13px 500 · sublabel `--t4` 11px

---

## 6. Segurança — V1.1

### 6.1 Renovação Automática do Token OAuth

**Problema atual:** o `access_token` do Google expira em 1 hora. O `refresh_token` é armazenado mas não usado. Resultado: backup falha com erro 401 se o app ficar aberto por mais de 1 hora.

**Solução:** antes de qualquer upload ao Drive, o backend Rust verifica se o token está próximo de expirar (`expires_at - now < 5min`). Se sim, usa o `refresh_token` para obter novo `access_token` silenciosamente antes de prosseguir.

**Onde implementar:** `src-tauri/src/backup.rs` — função de upload deve chamar `ensure_valid_token()` antes de qualquer requisição à API do Drive.

**Critério de conclusão:**
- App aberto por mais de 1 hora ainda realiza backup com sucesso
- O refresh ocorre sem interação do usuário
- Em caso de falha no refresh, status muda para "Erro de sync" com botão "Tentar novamente"

### 6.2 Armazenamento de Tokens em Cofre Nativo

**Problema atual:** `access_token` e `refresh_token` estão salvos em texto puro dentro de `finledger.db`. Qualquer software que copie o banco tem acesso ao Drive do usuário.

**Solução:** migrar o armazenamento de tokens para o cofre nativo do sistema operacional:
- Windows: Windows Credential Manager
- macOS: Keychain
- Linux: Secret Service (libsecret)

**Plugin:** `tauri-plugin-stronghold` ou `tauri-plugin-store` com encriptação. Avaliar qual oferece melhor suporte cross-platform no Tauri v2.

**Onde implementar:** `src-tauri/src/backup.rs` — funções de read/write de token passam a usar a API do plugin, não mais o SQLite.

**Critério de conclusão:**
- Tokens não aparecem em texto puro em `finledger.db`
- Conexão persiste após reiniciar o app
- Remoção do token ao desconectar funciona via cofre

### 6.3 Criptografia do Banco SQLite (SQLCipher)

**Problema atual:** `finledger.db` não tem senha. Qualquer pessoa com acesso ao sistema de arquivos consegue abrir e ler todas as transações.

**Solução:** ativar SQLCipher no `tauri-plugin-sql` (já suportado). O app gera ou deriva uma chave que só ele conhece e a usa para abrir o banco em todas as sessões.

**Nota:** A estratégia de geração/armazenamento da chave de encriptação precisa ser definida na implementação. Opções: chave derivada de UUID do dispositivo + salt fixo, ou chave armazenada no cofre nativo (seção 6.2).

**Critério de conclusão:**
- `finledger.db` não pode ser aberto por leitores SQLite externos sem a chave
- App abre e opera normalmente
- Bancos existentes de usuários da V1 são migrados na primeira abertura

---

## 7. Limpeza de Código — oauth.rs

Os itens abaixo produzem warnings no terminal e devem ser removidos ou anotados.

| Item | Linha | Ação |
|---|---|---|
| `constant REDIRECT_URI` | 14 | Remover — substituído por porta dinâmica na linha 110 |
| `struct OAuthToken` | 21 | Remover — substituído por `serde_json::Value` na linha 193 |
| `unused variable: oauth_state_clone` | 75 | Remover — validação migrada para `exchange_code_for_token_inner` |
| `tauri::command exchange_code_for_token` | 234 | Remover — troca de token agora ocorre internamente no Rust |
| `field exported_at is never read` | 488 | Adicionar `#[allow(dead_code)]` acima do campo em `RestoreBackupData` |

**Critério de conclusão:** `cargo check` sem warnings em `oauth.rs`.

---

## 8. Observabilidade — Logging Estruturado

**Problema atual:** o backend usa `eprintln!` espalhados sem padrão. Impossível rastrear falhas silenciosas de sync ou refresh de token.

**Solução:** adicionar `log` + `env_logger` ao `src-tauri/Cargo.toml`.

**Padrão de uso:**

```rust
log::info!("Iniciando escuta OAuth na porta {}", port);
log::debug!("Token expira em {}s, renovando...", seconds_left);
log::warn!("Retry de backup #{} após falha: {}", attempt, err);
log::error!("Falha crítica no restore: {}", err);
```

**Ativação em desenvolvimento:**

```bash
RUST_LOG=debug npm run tauri dev
RUST_LOG=finledger=debug,warn npm run tauri dev  # só logs do app
```

**Onde adicionar logs obrigatoriamente:**
- Início e fim de cada operação de sync/backup
- Cada refresh de token (com segundos restantes antes do refresh)
- Cada retry com número de tentativa e erro
- Início do listener OAuth e porta escolhida
- Resultado do restore (contagem de registros importados)

**Critério de conclusão:** `cargo check` passa, logs aparecem com `RUST_LOG=debug`, sem `eprintln!` em código de produção.

---

## 9. Testes Automatizados

### 9.1 Unitários — Backend Rust

**Foco principal:** módulo `recorrentes.rs` — lógica de geração de datas.

**Casos obrigatórios:**

| Cenário | Entrada | Esperado |
|---|---|---|
| Mensal, vários meses atrasados | `ultima_geracao = 2026-02-05`, `hoje = 2026-05-10`, `dia = 5` | Gera 05/03, 05/04, 05/05 |
| Dia 31 em mês curto | `dia = 31`, `mes = fev/2026` | Gera 28/02/2026 |
| Não gera antes de `criada_em` | `criada_em = 2026-05-01`, `ultima_geracao = null`, `hoje = 2026-05-10` | Gera apenas a partir de 01/05 |
| Pausada não gera | `ativa = 0` | Nenhuma transação gerada |
| Semanal domingo | `dia_semana = 0`, `ultima_geracao = 2026-05-11` (segunda) | Próxima geração em 17/05 (domingo) |
| Anual, mês e dia corretos | `frequencia = anual`, `dia = 15`, `mes = 3` | Gera apenas 15/03 de cada ano |

**Estrutura esperada:**

```rust
#[cfg(test)]
mod tests {
    #[test]
    fn test_mensal_varios_meses_atrasados() { ... }
    #[test]
    fn test_clamp_dia_31_fevereiro() { ... }
    // ...
}
```

### 9.2 Mock do Banco de Dados

**Objetivo:** testes que envolvem INSERT/SELECT/DELETE não devem tocar `finledger.db` do desenvolvedor.

**Implementação:** banco SQLite em memória (`:memory:`) injetado nos testes. O schema deve ser inicializado pelo mesmo `schema.rs` usado em produção.

```rust
fn test_db() -> Connection {
    let conn = Connection::open_in_memory().unwrap();
    run_migrations(&conn).unwrap();
    conn
}
```

**Casos que precisam de mock de banco:**
- `save_transacao` / `delete_transacao`
- `get_all_recorrentes` após insert
- Restore de backup (clear + insert transacional)

### 9.3 Testes E2E — WebdriverIO

**Objetivo:** simular um usuário real abrindo o app e executando os fluxos críticos.

**Fluxos obrigatórios na V1.1:**

| Teste | Passos | Validação |
|---|---|---|
| Criar transação | Abrir drawer → preencher campos → salvar | Toast "Transação salva!" aparece, linha aparece na tabela |
| Filtrar por mês | Selecionar mês anterior no filtro | Tabela mostra apenas transações do mês selecionado |
| Excluir transação | Clicar excluir → confirmar modal | Linha some da tabela |
| Criar recorrente mensal | Abrir drawer recorrente → preencher → salvar | Item aparece na lista com status "Ativa" |
| Pausar recorrente | Clicar pausar | Badge muda para "Pausada" |
| Criar categoria | Abrir modal → nome → cor → salvar | Categoria aparece na coluna correta |
| Exportar CSV | Selecionar período → clicar exportar | Diálogo nativo abre (mock) |

**Setup WebdriverIO:** seguir skill `testing-tauri-apps`. O executável Tauri é iniciado pelo runner antes de cada suite.

---

## 10. Critérios de Conclusão da V1.1

### Design Visual
- [ ] CSS variables trocados para o design system warm dark em todos os arquivos
- [ ] Fonte Inter carregada e aplicada (DM Serif Display e DM Sans removidos)
- [ ] Dashboard com layout de grid 4 colunas, gráfico de linha laranja, painel de categorias com percentual sobre receita
- [ ] Todas as telas com visual consistente ao design system

### Drawer
- [ ] Nova/Editar Transação funciona como drawer lateral (não mais tela separada)
- [ ] Nova/Editar Recorrente funciona como drawer lateral
- [ ] "Nova Transação" removido da sidebar
- [ ] Escape e clique no overlay fecham o drawer

### Segurança
- [ ] Refresh automático de token OAuth implementado e testado (app aberto > 1h ainda sincroniza)
- [ ] Tokens armazenados via cofre nativo (não mais em texto puro no SQLite)
- [ ] SQLite encriptado com SQLCipher
- [ ] Bancos existentes de V1 migrados na primeira abertura da V1.1

### Limpeza de Código
- [ ] `cargo check` em `oauth.rs` sem warnings
- [ ] Constante `REDIRECT_URI`, struct `OAuthToken`, variável `oauth_state_clone` e command `exchange_code_for_token` removidos
- [ ] `#[allow(dead_code)]` adicionado em `exported_at`

### Observabilidade
- [ ] `log` + `env_logger` adicionados ao Cargo.toml
- [ ] Logs em todos os fluxos críticos do backend
- [ ] Zero `eprintln!` em código de produção
- [ ] `RUST_LOG=debug npm run tauri dev` produz output legível

### Testes
- [ ] Todos os casos unitários de `recorrentes.rs` passando
- [ ] Mock de banco funcionando nos testes de CRUD
- [ ] Suite E2E com os 7 fluxos obrigatórios passando
- [ ] `npm run build` e `cargo check` passando

---

## 11. Ordem de Implementação Sugerida

```
T1  → Design system: tokens CSS + fonte Inter
T2  → Dashboard: redesign completo (cards, gráfico, categorias)
T3  → Drawer: Nova/Editar Transação
T4  → Tela Transações: visual + ícones + filtros
T5  → Drawer: Nova/Editar Recorrente
T6  → Tela Recorrentes: visual
T7  → Tela Categorias: visual + modal
T8  → Tela Importar/Exportar: visual + layout 2 colunas
T9  → Tela Backup: visual + formatação de timestamp
T10 → Limpeza oauth.rs
T11 → Logging estruturado
T12 → Renovação automática de token OAuth
T13 → Armazenamento de tokens em cofre nativo
T14 → SQLCipher (com migração de banco existente)
T15 → Testes unitários recorrentes.rs
T16 → Mock de banco nos testes de CRUD
T17 → Suite E2E WebdriverIO
T18 → Validação final: build + cargo check + smoke manual
```

**Dependências críticas:**
- T1 deve vir antes de T2–T9 (todos dependem dos tokens)
- T3 deve vir antes de T4 (drawer é referenciado na tela)
- T12, T13, T14 são independentes entre si mas todos dependem de T10 estar limpo
- T15–T17 podem ser feitos em paralelo após T1–T9

